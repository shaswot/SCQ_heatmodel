# SCQ_heatmodel
Heat modeling for large-scale superconducting quantum systems

### Running the docker container
```
# From the repo directory
docker compose up -d
```
This will build and run the docker container.
The notebook is accessible at localhost:1168
The password is `tqe` (specified in `docker-compose.yml`)
